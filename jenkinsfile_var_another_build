node() {
 
  currentBuild.result = "SUCCESS"
  try {
   def APP_URL='http://google.com'
        stage 'Checkout'
                checkout scm
    
        stage 'Health Check'
                build 'test2'
                    
  }
  catch(err) {
      currentBuild.result = "FAILURE"
      throw err
  }
}
